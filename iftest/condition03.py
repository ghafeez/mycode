#!/usr/bin/env python3
hostname = input("What value should we set for hostname?") # asking user's input

if hostname.lower() == "mtg":                       #defining the condition and lowering the case
    print("The hostname was found to be mtg")       #statement if input value matches condition
    print ("hostname matches expected config")      #printing matching statement
    print("exiting the script")                     #printing the exiting of script statement

